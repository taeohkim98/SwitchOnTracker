<?xml version="1.0" encoding="utf-8"?>
<!--
  [파일 역할]
  Android 앱 설정 파일
  - 정확한 알림 예약을 위한 권한 추가 (Android 12+)
  - 파일 접근 권한 (내보내기/가져오기)
  - 부팅 후 알림 재등록 (기기 재시작 시 예약 알림 복원)
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ① 정확한 시각에 알림 예약 (Android 12+ 필수) -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- ② 알림 표시 권한 (Android 13+ 필수) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <!-- ③ 배터리 절약 모드에서도 알림 발송 -->
    <uses-permission android:name="android.permission.USE_EXACT_ALARM" />

    <!-- ④ 파일 읽기/쓰기 (JSON 내보내기/가져오기) -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <!-- ⑤ 기기 재시작 후 알림 재등록 수신 -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED"/>
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:label="Switch-On Diet"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- 기기 재시작 후 알림 재예약을 위한 BroadcastReceiver -->
        <!-- flutter_local_notifications가 내부적으로 처리 -->
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationReceiver"/>
        <receiver android:exported="false"
            android:name="com.dexterous.flutterlocalnotifications.ScheduledNotificationBootReceiver">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED"/>
                <action android:name="android.intent.action.MY_PACKAGE_REPLACED"/>
                <action android:name="android.intent.action.QUICKBOOT_POWERON"/>
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON"/>
            </intent-filter>
        </receiver>

        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
</manifest>
